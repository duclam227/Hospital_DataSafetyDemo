<Page x:Class="GUI_HospitalManagement.ViewDataSet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:GUI_HospitalManagement"
        mc:Ignorable="d"
        Title="ViewDataSet" d:DesignWidth="900" d:DesignHeight="500">
    <Grid>
        <DataGrid Name="DataGridView" 
                  HeadersVisibility="All" 
                  RowHeaderWidth="60" 
                  AutoGenerateColumns="false" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã số role"
                                    Binding="{Binding ROLE_ID}"
                                    Width="*"/>
                <DataGridTextColumn Header="Tên role"
                                    Binding="{Binding ROLE}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Yêu cầu mật khẩu"
                                    Binding="{Binding PASSWORD_REQUIRED}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
